services:
  db:
    image: postgres:17.5
    restart: unless-stopped
    environment:
      POSTGRES_DB: hardhat_staging
      POSTGRES_USER: hardhat_user
      POSTGRES_PASSWORD: hardhat_password
      POSTGRES_INITDB_ARGS: "--auth-host=md5 --auth-local=trust"
    volumes:
      - postgres_staging_data:/var/lib/postgresql/data
    networks:
      - staging_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hardhat_user -d hardhat_staging"]
      interval: 10s
      timeout: 5s
      retries: 5

  web:
    image: image: ${DEPLOY_IMAGE:-hardhat-website:ci}
    restart: unless-stopped

    environment:
      DEBUG: "0"

      SECRET_KEY: jenkins-staging-secret-key

      DB_ENGINE: postgresql
      DB_NAME: hardhat_staging
      DB_USERNAME: hardhat_user
      DB_PASS: hardhat_password
      DB_HOST: db
      DB_PORT: "5432"

      PYTHONUNBUFFERED: "1"
      DJANGO_SETTINGS_MODULE: core.settings

    volumes:
      - staticfiles_staging:/app/static
      - mediafiles_staging:/app/media

    networks:
      - staging_network

    depends_on:
      db:
        condition: service_healthy

    command: >
      sh -c "python manage.py migrate &&
             python manage.py collectstatic --noinput &&
             python manage.py runserver 0.0.0.0:8000 --insecure"

  nginx:
    image: nginx:latest
    restart: unless-stopped

    ports:
      - "8082:80"

    volumes:
      - staticfiles_staging:/app/static:ro
      - mediafiles_staging:/app/media:ro
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro

    networks:
      - staging_network

    depends_on:
      - web

networks:
  staging_network:
    driver: bridge

volumes:
  staticfiles_staging:
  mediafiles_staging:
  postgres_staging_data: